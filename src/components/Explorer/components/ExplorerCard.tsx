import { Box } from "@mui/system";
import { styled } from "@mui/material";

export const ExplorerCard = styled(Box)({
  backgroundColor: "white",
  borderRadius: 15,
  padding: 15,
  boxShadow: "5px 5px 10px -2px rgba(0,0,0,0.1)",
});
